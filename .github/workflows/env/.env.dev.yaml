# Test sfrontend Environment Variables
name: sfrontend DEV Config
pipeline:
    enabled: true
branch:
    type: dev

# Global Flags for Quick Action
global:
    build: false                 # you  can optionally add '[no build]' to commit message to skip apim deploymen
    deploy: true                # you  can optionally add '[no deploy]' to commit message to skip apim deployment
    scan-containers: false      # you  can optionally add '[no grype]' to commit message to scan containers
    linting: false              # you  can optionally add '[no lint]' to commit message to skip linting

# Versioning Configuration
versioning:
  appType: angular
  appPath: ./frontend
  increaseVersion: true
  addBranchName: true
  commitVersion: true
  mergeMainToDev: true

# Build and Push Configuration
buildpushmatrix:
  - name: 'Sfrontend to Gitlab Container Registry'
    enabled: true
    build:
        name: npmjs
        buildArgs: ''
        dockerfile: './frontend/Dockerfile' 
        imagename: /splatforma/sfrontend
        imagetag: dev-github

# Create Application Configuration
createappmatrix:
  # - name: 'ArgoCD [Old Bucharest Cluster]'
  #   enabled: true
  #   cluster:
  #       name: dev_cluster
  #       argocdurl: 'k3s-argocd.wlasnasprawa.com'   
  #       namespace: 'default'
  #       argocdusername: 'ARGO_USERNAME_BUCHAREST'
  #       argocdpassword: 'ARGO_PASSWORD_BUCHAREST'
  #       appname: 'splatform-dev'
  #       sourcemanifest: 'splatform-dev.bucharest.yaml'

  - name: 'ArgoCD [Bucharest chmlro Cluster DEV]'
    enabled: true
    cluster:
        name: prd_cluster
        argocdurl: 'argocd-chmlro-wlsnprw-prd.wlasnasprawa.com'   
        namespace: 'default'
        argocdusername: 'ARGO_USERNAME_CHMLRO'
        argocdpassword: 'ARGO_PASSWORD_CHMLRO'
        appname: 'splatform-dev'
        sourcemanifest: 'splatform-dev.bucharest.yaml'

  - name: 'ArgoCD [Poznan Cluster - DEV]'
    enabled: true
    cluster:
        name: dev_cluster
        argocdurl: 'app-argocd-dev-poznan.wlasnasprawa.com'   
        namespace: 'default'
        argocdusername: 'ARGO_USERNAME_POZNAN'
        argocdpassword: 'ARGO_PASSWORD_POZNAN'
        appname: 'splatform-dev'
        sourcemanifest: 'splatform-dev.poznan.yaml'

# Deploy Configuration
deploymatrix:
  # - name: 'ArgoCD [Old Bucharest Cluster]'
  #   enabled: true
  #   cluster:
  #       name: dev_cluster
  #       argocdurl: 'k3s-argocd.wlasnasprawa.com'   
  #       namespace: 'default'
  #       argocdusername: 'ARGO_USERNAME_BUCHAREST'
  #       argocdpassword: 'ARGO_PASSWORD_BUCHAREST'
  #       appname: 'splatform-dev'
  #       parametername: 'splatformafrontend-app.pipelinedeployed'
  #       parametervalue: 'dev'
  #       appendtimestamp: true

  - name: 'ArgoCD [Bucharest chmlro Cluster DEV]'
    enabled: true
    cluster:
        name: prd_cluster
        argocdurl: 'argocd-chmlro-wlsnprw-prd.wlasnasprawa.com'   
        namespace: 'default'
        argocdusername: 'ARGO_USERNAME_CHMLRO'
        argocdpassword: 'ARGO_PASSWORD_CHMLRO'
        appname: 'splatform-dev'
        parametername: 'splatformafrontend-app.pipelinedeployed'
        parametervalue: 'dev'
        appendtimestamp: true

  - name: 'ArgoCD [Poznan Cluster - DEV]'
    enabled: true
    cluster:
        name: dev_cluster
        argocdurl: 'app-argocd-dev-poznan.wlasnasprawa.com'   
        namespace: 'default'
        argocdusername: 'ARGO_USERNAME_POZNAN'
        argocdpassword: 'ARGO_PASSWORD_POZNAN'    
        appname: 'splatform-dev'
        parametername: 'splatformafrontend-app.pipelinedeployed'
        parametervalue: 'dev'
        appendtimestamp: true

syncmatrix:
  # - name: 'ArgoCD [Old Bucharest Cluster] - Sync sfrontend App'
  #   enabled: true
  #   cluster:
  #       name: dev_cluster
  #       argocdurl: 'k3s-argocd.wlasnasprawa.com'   
  #       namespace: 'default'
  #       argocdusername: 'ARGO_USERNAME_BUCHAREST'
  #       argocdpassword: 'ARGO_PASSWORD_BUCHAREST'
  #       appname: 'splatform-dev'
  #       delaybeforesync: 90s

  - name: 'ArgoCD [Bucharest chmlro Cluster] - Sync sfrontend App'
    enabled: true
    cluster:
        name: dev_cluster
        argocdurl: 'argocd-chmlro-wlsnprw-prd.wlasnasprawa.com'   
        namespace: 'default'
        argocdusername: 'ARGO_USERNAME_CHMLRO'
        argocdpassword: 'ARGO_PASSWORD_CHMLRO'
        appname: 'splatform-dev'
        delaybeforesync: 90s

  - name: 'ArgoCD [Poznan Cluster - TST] - Sync sfrontend App'
    enabled: true
    cluster:
        name: dev_cluster
        argocdurl: 'app-argocd-dev-poznan.wlasnasprawa.com'   
        namespace: 'default'
        argocdusername: 'ARGO_USERNAME_POZNAN'
        argocdpassword: 'ARGO_PASSWORD_POZNAN'
        appname: 'splatform-dev'
        delaybeforesync: 90s

# Linting Configuration
lintmatrix:
    - name: Codebase Linting
      FILTER_REGEX_INCLUDE: ".*.*"
      FILTER_REGEX_EXCLUDE: ""
      DISABLE_ERRORS: true

# Logging Configuration
logging:
    level: debug
