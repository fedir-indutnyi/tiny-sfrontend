# Test sfrontend Environment Variables
name: sfrontend production Config
pipeline:
    enabled: true
branch:
    type: production

# Global Flags for Quick Action
global:
    build: true                 # you  can optionally add '[no build]' to commit message to skip apim deploymen
    deploy: true                # you  can optionally add '[no deploy]' to commit message to skip apim deployment
    scan-containers: false      # you  can optionally add '[no grype]' to commit message to scan containers
    linting: false              # you  can optionally add '[no lint]' to commit message to skip linting

# Versioning Configuration
versioning:
  appType: angular
  appPath: ./frontend
  increaseVersion: false
  addBranchName: false
  commitVersion: false
  mergeMainToDev: true

# Build and Push Configuration
buildpushmatrix:
  - name: 'Sfrontend to Gitlab Container Registry'
    enabled: true
    build:
        name: npmjs
        buildArgs: ''
        dockerfile: './frontend/Dockerfile' 
        imagename: /splatforma/sfrontend
        imagetag: production-github

# Create Application Configuration
createappmatrix:
  # - name: 'ArgoCD [Old Bucharest Cluster]'
  #   enabled: true
  #   cluster:
  #       name: dev_cluster
  #       argocdurl: 'k3s-argocd.wlasnasprawa.com'   
  #       namespace: 'default'
  #       argocdusername: 'ARGO_USERNAME_BUCHAREST'
  #       argocdpassword: 'ARGO_PASSWORD_BUCHAREST'
  #       appname: 'splatform-production'
  #       sourcemanifest: 'splatform-production.bucharest.yaml'

  - name: 'ArgoCD [Bucharest chmlro Cluster production]'
    enabled: true
    cluster:
        name: prd_cluster
        argocdurl: 'argocd-chmlro-wlsnprw-prd.wlasnasprawa.com'   
        namespace: 'default'
        argocdusername: 'ARGO_USERNAME_CHMLRO'
        argocdpassword: 'ARGO_PASSWORD_CHMLRO'
        appname: 'splatform-production'
        sourcemanifest: 'splatform-production.bucharest.yaml'

  - name: 'ArgoCD [Poznan Cluster - production]'
    enabled: true
    cluster:
        name: dev_cluster
        argocdurl: 'app-argocd-dev-poznan.wlasnasprawa.com'   
        namespace: 'default'
        argocdusername: 'ARGO_USERNAME_POZNAN'
        argocdpassword: 'ARGO_PASSWORD_POZNAN'
        appname: 'splatform-production'
        sourcemanifest: 'splatform-production.poznan.yaml'

# Deploy Configuration
deploymatrix:
  # - name: 'ArgoCD [Old Bucharest Cluster]'
  #   enabled: true
  #   cluster:
  #       name: dev_cluster
  #       argocdurl: 'k3s-argocd.wlasnasprawa.com'   
  #       namespace: 'default'
  #       argocdusername: 'ARGO_USERNAME_BUCHAREST'
  #       argocdpassword: 'ARGO_PASSWORD_BUCHAREST'
  #       appname: 'splatform-production'
  #       parametername: 'splatformafrontend-app.pipelinedeployed'
  #       parametervalue: 'production'
  #       appendtimestamp: true

  - name: 'ArgoCD [Bucharest chmlro Cluster production]'
    enabled: true
    cluster:
        name: prd_cluster
        argocdurl: 'argocd-chmlro-wlsnprw-prd.wlasnasprawa.com'   
        namespace: 'default'
        argocdusername: 'ARGO_USERNAME_CHMLRO'
        argocdpassword: 'ARGO_PASSWORD_CHMLRO'
        appname: 'splatform-production'
        parametername: 'splatformafrontend-app.pipelinedeployed'
        parametervalue: 'production'
        appendtimestamp: true

  - name: 'ArgoCD [Poznan Cluster - production]'
    enabled: true
    cluster:
        name: dev_cluster
        argocdurl: 'app-argocd-dev-poznan.wlasnasprawa.com'   
        namespace: 'default'
        argocdusername: 'ARGO_USERNAME_POZNAN'
        argocdpassword: 'ARGO_PASSWORD_POZNAN'    
        appname: 'splatform-production'
        parametername: 'splatformafrontend-app.pipelinedeployed'
        parametervalue: 'production'
        appendtimestamp: true

syncmatrix:
  # - name: 'ArgoCD [Old Bucharest Cluster] - Sync sfrontend App'
  #   enabled: true
  #   cluster:
  #       name: dev_cluster
  #       argocdurl: 'k3s-argocd.wlasnasprawa.com'   
  #       namespace: 'default'
  #       argocdusername: 'ARGO_USERNAME_BUCHAREST'
  #       argocdpassword: 'ARGO_PASSWORD_BUCHAREST'
  #       appname: 'splatform-production'
  #       delaybeforesync: 90s

  - name: 'ArgoCD [Bucharest chmlro Cluster] - Sync sfrontend App'
    enabled: true
    cluster:
        name: dev_cluster
        argocdurl: 'argocd-chmlro-wlsnprw-prd.wlasnasprawa.com'   
        namespace: 'default'
        argocdusername: 'ARGO_USERNAME_CHMLRO'
        argocdpassword: 'ARGO_PASSWORD_CHMLRO'
        appname: 'splatform-production'
        delaybeforesync: 90s

  - name: 'ArgoCD [Poznan Cluster - TST] - Sync sfrontend App'
    enabled: true
    cluster:
        name: dev_cluster
        argocdurl: 'app-argocd-dev-poznan.wlasnasprawa.com'   
        namespace: 'default'
        argocdusername: 'ARGO_USERNAME_POZNAN'
        argocdpassword: 'ARGO_PASSWORD_POZNAN'
        appname: 'splatform-production'
        delaybeforesync: 90s

# Linting Configuration
lintmatrix:
    - name: Codebase Linting
      FILTER_REGEX_INCLUDE: ".*.*"
      FILTER_REGEX_EXCLUDE: ""
      DISABLE_ERRORS: true

# Logging Configuration
logging:
    level: debug
